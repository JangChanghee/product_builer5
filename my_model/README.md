# Teachable Machine Model

This directory is intended to store the image classification model files exported from Google's Teachable Machine.

## Required Files

Please add the following files to this directory:

*   `model.json`
*   `metadata.json`
*   `weights.bin`

These files are generated when you export your model from the Teachable Machine editor.
